{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "scripts/**", "package.json", "tsconfig.json", "tsdown.config.ts"],
      "outputs": ["dist/**", "web-types.json", "web-tags.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
